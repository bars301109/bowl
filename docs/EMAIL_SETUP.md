# Настройка Email для сброса пароля (Gmail)

Для работы функции "Забыли пароль?" настроен Gmail по умолчанию.

## Быстрая настройка Gmail

### Шаг 1: Подготовка Gmail аккаунта

1. **Войдите в ваш Gmail аккаунт** или создайте новый
2. **Включите двухэтапную аутентификацию** (обязательно!):
   - Перейдите в https://myaccount.google.com/security
   - Найдите "Двухэтапная аутентификация" и включите её
   - Следуйте инструкциям для настройки

### Шаг 2: Создание пароля приложения

1. **Перейдите на страницу паролей приложений**:
   - https://myaccount.google.com/apppasswords
   - Или: Настройки Google → Безопасность → Пароли приложений

2. **Создайте новый пароль приложения**:
   - В разделе "Выберите приложение" выберите **"Почта"**
   - В разделе "Выберите устройство" выберите **"Другое (указать название)"**
   - Введите название: **"Akylman Quiz"**
   - Нажмите **"Создать"**

3. **Скопируйте 16-значный пароль** (без пробелов):
   - Пример: `abcd efgh ijkl mnop` → используйте `abcdefghijklmnop`

### Шаг 3: Настройка переменных окружения

#### Для локальной разработки:

Создайте файл `.env` в папке `backend/`:

```bash
# Gmail настройки
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=abcdefghijklmnop

# Или используйте стандартные переменные:
SMTP_USER=your-email@gmail.com
SMTP_PASS=abcdefghijklmnop
SMTP_FROM=your-email@gmail.com
```

#### Для Render (Production):

📖 **Подробная инструкция с скриншотами:** См. файл `docs/RENDER_GMAIL_SETUP.md`

**Краткая инструкция:**

1. Откройте https://dashboard.render.com
2. Найдите ваш сервис в списке "Services" и кликните на него
3. Перейдите на вкладку **"Environment"** (или "Переменные окружения")
4. Добавьте две переменные:

   **Переменная 1:**
   - **Key:** `GMAIL_USER`
   - **Value:** `your-email@gmail.com` (ваш реальный Gmail)

   **Переменная 2:**
   - **Key:** `GMAIL_APP_PASSWORD`
   - **Value:** `abcdefghijklmnop` (16-значный пароль приложения, без пробелов!)

5. Нажмите **"Save"** / **"Add"** для каждой переменной
6. Сервис автоматически перезапустится (или нажмите "Manual Deploy")

**Важно:** 
- Не добавляйте кавычки вокруг значений
- Пароль приложения вводите без пробелов
- После добавления подождите 1-2 минуты для перезапуска

### Шаг 4: Проверка работы

1. Зайдите на страницу входа
2. Нажмите **"Забыли пароль?"**
3. Введите email зарегистрированной команды
4. Проверьте почту (включая папку "Спам")
5. Вы должны получить письмо с 6-значным кодом

## Важные замечания

⚠️ **Не используйте обычный пароль Gmail!** Используйте только пароль приложения.

✅ **Пароль приложения безопасен** - его можно отозвать в любой момент

✅ **Коды действительны 15 минут** и одноразовые

✅ **Если email не зарегистрирован**, система не сообщит об этом (для безопасности)

## Устранение проблем

### Письма не приходят:

1. Проверьте папку "Спам"
2. Убедитесь, что двухэтапная аутентификация включена
3. Проверьте, что используете пароль приложения, а не обычный пароль
4. Проверьте логи сервера на наличие ошибок

### Ошибка "Invalid login":

- Убедитесь, что используете полный email (с @gmail.com)
- Проверьте, что пароль приложения скопирован правильно (без пробелов)

### Ошибка "Less secure app":

- Gmail больше не поддерживает "менее безопасные приложения"
- Обязательно используйте пароль приложения (см. Шаг 2)

## Альтернативные варианты (опционально)

Если Gmail не подходит, можно использовать другие сервисы. Для этого установите соответствующие переменные окружения:

### SendGrid (рекомендуется для production)

1. Зарегистрируйтесь на https://sendgrid.com (бесплатный план: 100 emails/день)
2. Создайте API ключ
3. Установите переменные окружения:
```bash
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key
SMTP_FROM=noreply@yourdomain.com
```

### Вариант 3: Mailgun

1. Зарегистрируйтесь на https://www.mailgun.com
2. Получите SMTP credentials
3. Установите переменные окружения:
```bash
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-mailgun-username
SMTP_PASS=your-mailgun-password
SMTP_FROM=noreply@yourdomain.com
```

### Вариант 4: Resend (современный вариант)

1. Зарегистрируйтесь на https://resend.com
2. Получите API ключ
3. Используйте SMTP:
```bash
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=resend
SMTP_PASS=your-resend-api-key
SMTP_FROM=noreply@yourdomain.com
```

## Настройка на Render

1. Перейдите в настройки вашего сервиса на Render
2. Добавьте переменные окружения в раздел "Environment":
   - `SMTP_HOST`
   - `SMTP_PORT`
   - `SMTP_SECURE`
   - `SMTP_USER`
   - `SMTP_PASS`
   - `SMTP_FROM`

3. Сохраните и перезапустите сервис

## Проверка работы

После настройки попробуйте:
1. Зайти на страницу входа
2. Нажать "Забыли пароль?"
3. Ввести email зарегистрированной команды
4. Проверить почту (включая спам)

## Безопасность

- Коды сброса пароля действительны 15 минут
- Коды можно использовать только один раз
- Старые коды автоматически удаляются при создании нового
- Если email не зарегистрирован, система не сообщает об этом (для безопасности)

